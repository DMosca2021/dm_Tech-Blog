<div class='text-center' id="blog-card">
  <h2>{{name}}</h2>
  <p>
    <span class='subject-text'>{{subject}}</span>
  </p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>

  <p>
    <button class='btn btn-primary'>Edit this Blog</button>
  </p>
</div>